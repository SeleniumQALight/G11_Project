package org.pages;

import org.openqa.selenium.WebDriver;

public class ParentPage extends CommonActionsWithElements{ //create constructor
    public ParentPage(WebDriver webDriver) {
        super(webDriver);
    }
}
