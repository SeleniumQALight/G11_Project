package org.pages;

import org.openqa.selenium.WebDriver;

public class ParrentPage extends CommonActionsWithElements {
    public ParrentPage(WebDriver webDriver) {
        super(webDriver);
    }
}
